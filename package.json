{
  "name": "mono",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "tsdx": "^0.14.1",
    "typescript": "^4.3.4"
  },
  "workspaces": [
    "modules/sdk",
    "modules/txservice",
    "modules/contracts",
    "modules/router",
    "modules/utils",
    "modules/test-ui"
  ],
  "scripts": {
    "lerna": "lerna",
    "watch": "lerna run watch --stream --parallel",
    "test": "lerna run test --",
    "lint": "lerna run lint -- --fix",
    "clean": "rm -rf node_modules* && rm -rf modules/*/node_modules* modules/*/dist* modules/*/.nyc_output* modules/*/artifacts* modules/*/cache* modules/*/typechain*",
    "build": "lerna run build",
    "prepublish": "lerna run prepublish",
    "publish": "lerna publish --access=public"
  },
  "resolutions": {
    "**/typescript": "^4.3.4"
  },
  "dependencies": {
    "eslint": "^7.29.0",
    "start-server-and-test": "^1.12.5"
  }
}
