import { ProtocolStack } from "../helpers";

export const getProtocolStack = (network: "testnet" | "mainnet", env: "staging" | "prod"): ProtocolStack => {
  throw new Error("Not implemented yet");
};
