describe("Operation:status", () => {
  describe("#sendStatusToSequencer", () => {
    it("send router status to the sequencer successfully", () => {
      // await sendHeartbeat;
    });
    it("should throw if sending failed", () => {});
    it("should fail to send router status", () => {});
  });
});
