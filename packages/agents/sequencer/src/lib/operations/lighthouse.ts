import { LightHouseData, RequestContext } from "@connext/nxtp-utils";

export const storeLightHouseData = async (
  lighthouseData: LightHouseData,
  requestContext: RequestContext,
): Promise<void> => {};
