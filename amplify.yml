version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - yarn
        build:
          commands:
            - yarn build:all
            - yarn workspaces focus --production @connext/nxtp-test-ui
      artifacts:
        baseDirectory: packages/test-ui/build
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
